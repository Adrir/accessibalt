#JSGF V1.0;

/**
 * JSGF Grammar for Hello World example
 */

grammar hello;

public <greet> = (Good morning | Hello ) ( Rob | Asuna );
public <shutdown> = (Time for Bed) (Rob | Asuna);
public <letter> = (a | b | c);
public <instruction> = ( Rob | Asuna ) ( Say ) ( Please | Thank you );
